# Tomcat
server:
  port: 9203

# Spring
spring:
  application:
    # 应用名称
    name: xingqiao-system
  profiles:
    # 环境配置
    active: dev
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 47.115.48.217:9003
        ip: 127.0.0.1
        username: nacos
        password: Zding2025!
      config:
        # 配置中心地址
        server-addr: 47.115.48.217:9003
        username: nacos
        password: Zding2025!
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}

  servlet:
    multipart:
      max-file-size: 100

#打印sql
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
logging:
  level:
    com.xingqiao.system.mapper: debug
