<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xingqiao.system.mapper.SysFileExtMapper">

    <delete id="deleteSysFileByBusinessId">
        delete from sys_file where business_id in
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    
    <insert id="batchSysFile">
        insert into sys_file (user_id, business_id, business_type, file_type, file_url, sort_order, remark, create_by, create_time, update_by, update_time) values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.userId},#{item.businessId},#{item.businessType},#{item.fileType},#{item.fileUrl},#{item.sortOrder},#{item.remark},#{item.createBy},#{item.createTime},#{item.updateBy},#{item.updateTime})
        </foreach>
    </insert>
</mapper>